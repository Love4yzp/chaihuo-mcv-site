## 2026-04-28
- Kept `src/app/components/CrewManifest.tsx` intact for the next cleanup step, per instruction.
- Added a lightweight public SVG avatar for `xiaomei` so the required `image` schema field stays valid without touching existing team members.
- Migrated boarding entries to the new `id`/`location` structure and unified `crewId` values to `team.json` IDs.
